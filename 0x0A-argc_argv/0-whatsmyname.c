#include "main.h"
#include <stdio.h>

/**
 * main - prints the name
 * @argc - number of arguments
 * @args - array of arguments
 * Return always zero
 */

int main (int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv [0]);

	return (0);
}
